/**
 * agents/index.ts — convenience barrel for new code.
 * Existing consumers import from ./agent-manager.js; this file
 * re-exports everything for callers who prefer the directory import.
 */
export * from './agent-manager.js'
